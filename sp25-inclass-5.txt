1. Hexvalues values represent values for red, green, and blue in haxadecimal code.
2. Typography can be defined as “the art and technique of arranging type in order to make languages visible
3. The font-face rule sets a new font to use that is made via font family
4. Object-fit shorthand property allows you to set all the list style properties at once
5. The object-fit CSS property sets how the content of a replaced element, such as an <umg> or <video>, should be resized to fit its container. 
6. Create a webpage with an image element and image file. Set the size of this image     element to 1000px by 1000px and scale the image to fill the element while respecting the aspect ratio of the image.